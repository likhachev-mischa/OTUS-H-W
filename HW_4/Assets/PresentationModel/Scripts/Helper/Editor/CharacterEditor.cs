using UnityEditor;
using UnityEngine;

namespace MVVM
{
   /*[CustomEditor(typeof(Character))]
    public sealed class CharacterEditor : Editor
    {
        public readonly string[] statNames;
        public override void OnInspectorGUI()
        {
            base.OnInspectorGUI();
            
            var character = (Character) target;

            if (GUILayout.Button("Add Stat"))
            {
                //var stat = new CharacterStat(character.statName, character.statValue);
                //character.CharacterInfo.AddStat(stat);
            }
            
            
            name = GUILayout.TextField(name);
        }
        

    }*/
}